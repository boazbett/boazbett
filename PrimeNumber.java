//this program checks if number is prime

import java.util.Scanner;

class PrimeNumber {
    public static void main(String[] args) {
        Scanner input = new Scanner(System.in);
        int num = int.PrimeNumber;
        System.out.print("Enter a number to check PRIMITY!: ");
        
        while (num !=0){
            num = Scanner.nextInt();
            System.out.printf("Does%d is prime? %s %s %s %n", num, isPrimeOrNot(num), isPrimeNumber(num));
        }    
    }
}